---
export const textStyles = {
  banner: 'text-neutral-50 text-3xl md:text-5xl font-title uppercase', // Mobile: 40px | Desktop: 88px
  title: 'text-neutral-50 text-2xl md:text-3xl font-title uppercase', // Mobile: 28px | Desktop: 40px
  bigNumber: 'text-neutral-50 text-2xl md:text-4xl font-title', // Mobile: 28px | Desktop: 70px
  bodyBanner: 'text-primary-400 text-sm md:text-xl font-body', // Mobile: 14px | Desktop: 24px
  body: 'text-neutral-50 text-sm md:text-lg font-body', // Mobile: 14px | Desktop: 20px
  button: 'text-neutral-50 text-sm md:text-lg font-body font-bold', // Mobile: 14x | Desktop: 20px
  badge: 'text-neutral-50 text-xs md:text-md font-body', // Mobile: 11px | Desktop: 16px
  cardTitle: 'text-neutral-50 text-sm md:text-xl font-title', // Mobile: 14px | Desktop: 24px
  cardBody: 'text-neutral-50 text-xs md:text-md font-body', // Mobile: 11px | Desktop: 16px
}

type Variant = keyof typeof textStyles
type TagType =
  | 'p'
  | 'a'
  | 'span'
  | 'small'
  | 'div'
  | 'strong'
  | 'em'
  | 'blockquote'
  | 'summary'
  | 'label'
  | 'h1'
  | 'h2'
  | 'h3'
  | 'h4'
  | 'h5'
  | 'h6'

const variantToTagMap: Partial<Record<Variant, TagType>> = {
  banner: 'h1',
  title: 'h2',
  cardTitle: 'h3',
}

const variantToColorMap: Partial<Record<Variant, string>> = {
  bodyBanner: 'text-primary-400'
}

type TextColor = keyof typeof variantToColorMap | 'none'

export interface Props {
  tag?: TagType
  variant?: Variant
  class?: string
  for?: string
  id?: string
  href?: string
  color?: TextColor
}

const {
  tag = 'p', // default to paragraph
  variant = 'body', // default to textMD variant
  class: className = '',
  for: htmlFor, // Capture 'for' attribute
  ...rest
} = Astro.props

const Tag: TagType = tag !== 'p' ? tag : variantToTagMap[variant] || tag

// Get the base classes for the variant
const baseClasses = textStyles[variant] || textStyles.body // Default to body if not found
---

<Tag
  class:list={[baseClasses, className]}
  for={htmlFor}
  {...rest}>
  <slot name='left-icon' />
  <slot />
  <slot name='right-icon' />
</Tag>
